# ============================================================
#  配置模板 —— 复制成 application-dev.yml 后改成你自己的值
#
#    cp application-dev.yml.example application-dev.yml
#
#  真正的 application-dev.yml 已被 .gitignore 排除，不会被提交。
# ============================================================
sky:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    host: localhost
    port: 3306
    database: sky_take_out
    username: root
    password: 你的MySQL密码

  # 阿里云 OSS：用于菜品/套餐图片上传（/admin/common/upload）
  # 本地不发图片的话可以先留占位值，上传接口会失败，但前端会自动降级成「手填图片地址」
  alioss:
    endpoint: oss-cn-你的地域.aliyuncs.com
    access-key-id: 你的AccessKeyId
    access-key-secret: 你的AccessKeySecret
    bucket-name: 你的Bucket名称

  redis:
    host: localhost
    port: 6379
    password: 你的Redis密码
    database: 10

  # 微信小程序：只有用户端登录（code 换 openid）和微信支付会用
  # 本仓库只做管理端，这两个值留占位即可
  wechat:
    appid: 你的小程序AppID
    secret: 你的小程序AppSecret
